backend:
  name: git-gateway
  branch: main

media_folder: "assets/images/uploads"
public_folder: "/assets/images/uploads"

# Retire site_url ak display_url pou evite konfli lokal
# Netlify ap detekte yo otomatikman

editor:
  preview: false

collections:
  - name: "products"
    label: "🛒 Tout Pwodwi"
    folder: "_products"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Titre du produit", name: "title", widget: "string" }
      - { label: "Catégorie", name: "category", widget: "string" }
      - { label: "Prix Actuel (HTG)", name: "price", widget: "number" }
      - { label: "Ancien Prix (Vente Flash)", name: "old_price", widget: "number", required: false }
      - { label: "Vente Flash ?", name: "flash_sale", widget: "boolean", default: false }
      - { label: "Image principale", name: "image", widget: "image" }
      - label: "Galerie d'images"
        name: "images"
        widget: "list"
        required: false
        field: { label: "Image", name: "img", widget: "image" }
      - { label: "Description", name: "body", widget: "markdown" }

  - name: "categories"
    label: "🗂 Catégories"
    folder: "_categories"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Nom de la Catégorie", name: "title", widget: "string" }
      - { label: "Description", name: "description", widget: "text" }
      - { label: "Image Bulle (Accueil)", name: "image", widget: "image" }
      - { label: "Layout", name: "layout", widget: "hidden", default: "category" }
