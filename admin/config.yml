backend:
  name: git-gateway
  branch: main

media_folder: "assets/images/uploads"
public_folder: "/assets/images/uploads"

site_url: "https://boutique-piyay.netlify.app"
display_url: "https://boutique-piyay.netlify.app"

editor:
  preview: false

collections:
  # 🛒 KOLEKSYON PWODWI
  - name: "products"
    label: "🛒 Produits"
    folder: "_products"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Titre du produit", name: "title", widget: "string" }
      - { label: "Catégorie", name: "category", widget: "string", hint: "Mete menm non ak kategori ou kreye a (ex: Habillement)" }
      - { label: "Prix (HTG)", name: "price", widget: "number" }
      - { label: "Image principale", name: "image", widget: "image" }
      - { label: "Vente Flash ?", name: "flash_sale", widget: "boolean", default: false }
      - { label: "Description détaillée", name: "body", widget: "markdown" }

  # 🗂 KOLEKSYON KATEGORI (DINAMIK)
  - name: "categories"
    label: "🗂 Catégories"
    folder: "_categories"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Nom de la Catégorie", name: "title", widget: "string" }
      - { label: "Description de la page", name: "description", widget: "text" }
      - { label: "Image Bulle (Accueil)", name: "image", widget: "image" }
      - { label: "Image Banner (Page Catégorie)", name: "banner_image", widget: "image" }
      - { label: "Layout", name: "layout", widget: "hidden", default: "category" }

  # 📢 PUBLICITÉS
  - name: "promotions"
    label: "📢 Publicités & Promos"
    folder: "_promotions"
    create: true
    fields:
      - { label: "Nom du Partenaire", name: "title", widget: "string" }
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Position", name: "position", widget: "select", options: ["top", "middle", "bottom"] }
      - { label: "Actif", name: "active", widget: "boolean", default: true }
